apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: nvcl-db
  name: nvcl-db
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
