
[project]
# PEP 621 project metadata
# See https://www.python.org/dev/peps/pep-0621/
dependencies = ["pandas", "numpy", "matplotlib", "wordcloud", "periodictable", "seaborn", "xmltodict", "requests", "nvcl-kit", "fpdf2", "pillow", "cython"]
requires-python = ">=3.10"

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"
